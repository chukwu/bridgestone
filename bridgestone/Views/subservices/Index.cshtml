@model IEnumerable<bridgestone.Models.subservice>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Backend.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.services.servicename)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.servicename)
            </th>
            <th></th>
        </tr>
    </thead>

    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.services.servicename)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.servicename)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ssid }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ssid }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ssid })
                </td>
            </tr>
        }
    </tbody>


</table>
